'use strict';
const fs = require('fs');
const papa = require ('papaparse');
const openai = require('openai');

module.exports.collector = async (event) => {
    console.log("invoked")

    console.log("This is where the magic should happen")
    
}